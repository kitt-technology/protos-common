version: 2
workflows:
  version: 2
  ci:
    jobs:
      - test: {}
jobs:
  test:
    working_directory: /go/src/github.com/kitt-technology/protos-common
    docker:
      - image: kittoffices/protoc-gen-graphql

    steps:
      - checkout
      - run: go get ./... | go install . | go test ./common
